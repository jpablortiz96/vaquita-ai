# Get your App ID from https://privy.io
NEXT_PUBLIC_PRIVY_APP_ID=clxxxxxxxxxxxxxxxxxx

# Public URL of the agent backend (Render). Used for future agent API calls.
# Locally you can leave this unset (defaults to http://localhost:3001).
NEXT_PUBLIC_AGENT_URL=https://your-backend.onrender.com
